# Optional: override the DummyJSON base URL, e.g. to point at a local proxy or a
# mock server returning /http/500 for error-path testing. Defaults to
# https://dummyjson.com when unset.
VITE_API_BASE_URL=https://dummyjson.com
